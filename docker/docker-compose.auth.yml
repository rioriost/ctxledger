# Deprecated: direct app-layer auth override has been removed.
#
# Authentication for ctxledger is now expected to be enforced at the proxy layer
# via docker/docker-compose.small-auth.yml.
#
# This file is intentionally left as a compatibility stub so older references
# fail safely without re-enabling deprecated direct-backend auth behavior.
