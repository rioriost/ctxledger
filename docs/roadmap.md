ctxledger roadmap

0.1
- workflow kernel
- workspace registry
- MCP tools basic

0.2
- episodic memory
- `memory_remember_episode` is the most direct tool fit
- `memory_get_context` may gain an initial episode-oriented form

0.3
- pgvector semantic search
- `memory_search` is the most direct tool fit
- `memory_get_context` may gain stronger relevance-based retrieval

0.4
- hierarchical memory retrieval
- `memory_get_context` may evolve into a more multi-layer context assembly surface

0.0
- stable runtime
