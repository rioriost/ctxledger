from __future__ import annotations

raise RuntimeError(
    "The local resume projection writer was removed in v0.5.3. "
    "Use canonical workflow resume interfaces instead of repository-local `.agent` projections."
)
